#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,b,c,count=0,i=1;
    printf("enter DECIMAL NUMBER");
    scanf("%d",&n);
    while(n!=0)
    {
       b=n%2;
       n=n/2;

              count=b*i+count;

              i=i*10;
}
 printf("%d",count);

return 0;
}
